{
  "$schema": "../schemas/capability-schema.json",
  "identifier": "default",
  "description": "Default capabilities for the Antigravity application",
  "windows": ["main"],
  "permissions": [
    "core:default",
    "shell:default",
    "core:window:default",
    "core:app:default",
    "core:event:default"
  ]
}
